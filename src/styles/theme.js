import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#800000",
    },
    secondary: {
      main: "#FFD700",
    },
    info: {
      main: "#A020F0",
    },
  },
});

export { theme };
