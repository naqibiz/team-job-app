import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#800000",
    },
    secondary: {
      main: "rgb(0, 82, 73)",
    },
    info: {
      main: "#A020F0",
    },
  },
});

export { theme };
